<?xml version="1.0" encoding="UTF-8"?>
<solver>
    <solutionClass>
        com.schoolplanner.timetable.domain.TimeTable
    </solutionClass>

    <entityClass>
        com.schoolplanner.timetable.domain.Lesson
    </entityClass>

    <scoreDirectorFactory>
        <constraintProviderClass>
            com.schoolplanner.timetable.solver.TimeTableConstraintProvider
        </constraintProviderClass>
    </scoreDirectorFactory>

    <termination>
        <terminationCompositionStyle>OR</terminationCompositionStyle>
        <secondsSpentLimit>400</secondsSpentLimit>
        <termination>
            <terminationCompositionStyle>AND</terminationCompositionStyle>
            <bestScoreLimit>0hard/-2147483648soft</bestScoreLimit>
            <unimprovedSecondsSpentLimit>15</unimprovedSecondsSpentLimit>
        </termination>
    </termination>

    <constructionHeuristic>
        <constructionHeuristicType>FIRST_FIT</constructionHeuristicType>
    </constructionHeuristic>
    <localSearch>
        <unionMoveSelector>
            <changeMoveSelector><valueSelector variableName="timeSlot"/></changeMoveSelector>
            <changeMoveSelector><valueSelector variableName="room"/></changeMoveSelector>

            <cartesianProductMoveSelector>
                <changeMoveSelector><valueSelector variableName="timeSlot"/></changeMoveSelector>
                <changeMoveSelector><valueSelector variableName="room"/></changeMoveSelector>
            </cartesianProductMoveSelector>

            <pillarChangeMoveSelector>
                <subPillarType>ALL_OR_NOTHING</subPillarType>
                <valueSelector variableName="timeSlot"/>
            </pillarChangeMoveSelector>

            <swapMoveSelector/>
        </unionMoveSelector>
        <localSearchType>TABU_SEARCH</localSearchType>
    </localSearch>
</solver>